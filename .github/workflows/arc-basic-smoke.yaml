name: Probar Runner de ARC

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    # ¡La magia! Usa las etiquetas del RunnerDeployment
    # Debe incluir "self-hosted" y al menos una etiqueta personalizada
    runs-on: [self-hosted, alesia-runners]

    steps:
      - name: Verificar el runner
        run: |
          echo "¡Hola desde mi runner en Kubernetes (ARC)!"
          echo "Este trabajo corre en el runner: $RUNNER_NAME"

      - name: Checkout
        uses: actions/checkout@v4

      - name: Ejecutar un build (ejemplo)
        run: |
          echo "Simulando un build..."
          sleep 5
          echo "Build completado."
